<theme>
  <name>Futuristic Cyberpunk Hacker Theme</name>
  <description>A modern UI theme featuring neon colors and futuristic styles.</description>
  <styles>
    <style>
      body {
        background-color: #000;
        color: #0ff;
        font-family: 'Roboto', sans-serif;
      }
      .neon {
        text-shadow: 0 0 5px #0ff, 0 0 10px #0ff;
        animation: flicker 1.5s infinite alternate;
      }
      @keyframes flicker {
        0% { opacity: 1; }
        100% { opacity: 0.7; }
      }
    </style>
  </styles>
  <components>
    <component>
      <name>Button</name>
      <description>A modern button component with hover effects.</description>
      <code>
        import React from 'react';
        const Button = () => {
          return <button className='neon'>Click Me</button>;
        };
        export default Button;
      </code>
    </component>
    <component>
      <name>Card</name>
      <description>A card component for displaying content with depth.</description>
      <code>
        import React from 'react';
        const Card = ({ title, content }) => {
          return (
            <div className='card neon'>
              <h2>{title}</h2>
              <p>{content}</p>
            </div>
          );
        };
        export default Card;
      </code>
    </component>
  </components>
</theme>